import React from 'react'
import { connect } from 'react-redux'

import './PostItemList.scss'

const PostItemList = props => {
  return (
    <div classname="post-item-list">
      <div ></div>
    </div>
  )
}

const mapStateToProps = state => {}
export default connect(mapStateToProps)(PostItemList)
